import styled from 'styled-components/native';

export const RegisterView = styled.View`
  background-color: ${({ theme }) => theme.colors.background};
`;

export const RegisterOptionView = styled.View`
  margin-top: 15px;
`;
